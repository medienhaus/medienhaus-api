# stechlin-api

### Development

1. Copy `config.js.example` to `config.js` and add your credentials
2. Install dependencies via `npm clean-install`
3. Start development server via `npm run start:dev`
4. Open `http://localhost:3004`
